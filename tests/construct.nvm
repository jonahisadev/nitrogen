_Color:
	malloc 12
	load erx
	get eax (ebp)+8
	get ebx (ebp)+12
	get ecx (ebp)+16
	set (erx) eax
	set (erx)+4 ebx
	set (erx)+8 ecx
	ret

_Color_:
	malloc 12
	load erx

	get eax (ebp)+8
	set (erx) eax

	get eax (ebp)+12
	set (erx)+4 eax

	get eax (ebp)+16
	set (erx)+8 eax

	ret