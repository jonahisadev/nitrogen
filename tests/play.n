a: i32!
b: i32 = 1209

x: i32*!                # Creates an array object

struct Color:
    r: i32!
    g: i32!
    b: i32!
end

color_test: Color!

func main()[i32]:
    color_test = Color(0, 128, 255)
    x = [5]

    delete(color_test)
    delete(x)

    a = 30 + 12
    return a
endf