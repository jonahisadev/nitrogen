a: i32!
b: i32 = 1209

struct Color:
    r: i32!
    g: i32!
    b: i32!
end

test_color: Color!

func this_does_something()[i32]:
    test_color = Color(0, 0, 255)
    return 0
endf

func main()[i32]:
    this_does_something()
    delete(test_color)

    a = 30 + 12
    return a
endf